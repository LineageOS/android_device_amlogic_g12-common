# g12a
# Mali-G31 blob, also used by SM1. Extract with:
#   ./extract-files.py -s g12a <g12a/sm1 dump>
# Installed into its own sub directory instead of straight into vendor/lib/egl:
# that keeps it from sharing an install path with the g12b blob. The real
# vendor/lib/egl/libGLES_mali.so is an install_symlink, see Android.bp.
vendor/lib/egl/libGLES_mali.so:vendor/lib/egl/g12a/libGLES_mali.so;MODULE=libGLES_mali_g12a
